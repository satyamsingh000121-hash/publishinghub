# Database configuration (Default is file-based SQLite database, switchable to PostgreSQL or MySQL)
DATABASE_URL="file:./dev.db"

# Authentication & Security Secrets
JWT_SECRET="publishinghub_super_secret_jwt_key_2026_change_in_production"
JWT_EXPIRES_IN="7d"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional Email / Notification service
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="noreply@publishinghub.com"
SMTP_PASS=""
